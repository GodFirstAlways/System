-- RainInforced UI Module
local UI = {}
local Players = game:GetService("Players")
local plr = Players.LocalPlayer

-- Create main GUI
local SysBroker = Instance.new("ScreenGui")
SysBroker.Name = "SysBroker"
SysBroker.Parent = game.CoreGui

-- Background
local Background = Instance.new("ImageLabel")
Background.Name = "Background"
Background.Parent = SysBroker
-- ... (copy all your background properties)

-- Create all buttons, sections, inputs
-- ... (copy all your UI creation code from lines 300-2000 of original script)

-- Export everything
UI.SysBroker = SysBroker
UI.Background = Background
UI.Buttons = {
    -- All buttons here
}
UI.Sections = {
    -- All sections here
}
UI.Inputs = {
    -- All inputs here
}
UI.Labels = {
    -- All labels here
}
UI.Images = {
    -- All images here
}

return UI